basePath: /
info:
  contact: {}
  description: This Simple CRUD server
  title: CRUD Users
  version: "1.0"
paths:
  /users:
    get:
      consumes:
      - application/json
      description: List All Users
      produces:
      - application/json
      responses:
        "200":
          description: string
          schema:
            type: string
      tags:
      - json
  /users/{id}:
    get:
      consumes:
      - application/json
      parameters:
      - description: User ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: string
          schema:
            type: string
      tags:
      - json
swagger: "2.0"
